# 独立质量监督基线 v1

冻结时间：2026-08-30T22:26:48+08:00
监督角色：`independent_e2e_quality_supervisor`
结论：`COMPARISON PENDING / LOCAL RELEASE NO-GO`

## 已完成的盲轨

三条 v1 的盲性复审发现旧 `samples.json` 含候选来源子类判断，因此已降级为 `frozen_with_prior`，只作探索性基线。随后通过 `fork_turns=none` 的全新隔离评测上下文完成 v2；隔离者仅获原视频、媒体 SHA、schema 和用户明确业务真值，访问证明未报告违规。v2 已作为严格 `frozen_blind` 基准冻结；v1 永久保留为审计轨迹。

盲性披露：冻结前监督者读取了样片打包目录中的 `manifest.json`、`README.md` 和 `samples.json`。其中含人工业务标签、媒体指纹及旧的候选视觉初判，但不含本轮被测系统输出。为避免把该先验隐藏成“纯盲”，每份 golden 均在 `blindness` 中显式登记；来源结论仍保持待原片/血缘核验，QS-GH-002 的子类只按用户明确业务先验记录。

| case_id | 题材 | 业务标签 | 来源子类结论 | Golden | 留人专家先验 |
|---|---|---|---|---|---:|
| QS-GH-001 | 见 v2 结构化字段 | 外搭正例 | 无原片/血缘时待复核 | `27ded9d8…45bc` | 见 v2 |
| QS-GH-002 | 见 v2 结构化字段 | 外搭正例 | 人物资产复用困难正例（用户业务真值；镜头证据待补） | `240ce251…251` | 见 v2 |
| QS-GH-003 | 见 v2 结构化字段 | 外搭正例 | 无原片/血缘时待复核 | `749df05d…dfb7` | 见 v2 |

这三个留人分是监督者对输入素材的独立标准答案，不是对系统输出的得分，也不能用来宣称系统达标。

## 第一轮专家标准要点

### QS-GH-001

前 10 秒建立“17 岁未婚怀孕并靠捡垃圾养家”，10–32 秒完成“为儿子婚姻倾尽积蓄→被儿媳赶走→流落街头→救下首富之女”，36–48 秒建立首富父亲隐瞒身份参加相亲。核心承诺是受辱母亲通过“隐形富豪”关系获得情感与身份逆转。候选钩子范围为 0–42 秒，扩展范围 0–90 秒；真实边界尚未核验。

### QS-GH-002

前 10 秒为流浪儿童受暴力，10–36 秒是饥饿与地面食物羞辱，40–63 秒揭示母亲因头部创伤失忆、同样流浪，并让高地位男子进入救助线。后续发展到母子重逢与男子认出母亲。核心承诺是母子身份与家庭修复。该样本必须作为“视觉/人物连续仍为外搭”的困难正例；不得用人物相似性否掉外搭标签。

### QS-GH-003

0–20 秒完成古代王后受袭、落水未死、穿越约两千年成为现代富豪妻子的高概念建立；20–34 秒通过吊灯误认与“我是王后”强化时代错位；34–74 秒建立现代丈夫 Austin、Caroline 落水指控与现代日期确认。核心承诺是古代王后利用身份/能力逆转现代豪门婚姻中的弱势地位。

## 不能评分的部分

当前没有找到以下与这三个 case 对应的权威输入/输出，因此未打开对照轨，也没有生成伪分数：

- 对应原剧的连续前 10 集和剧目 ID；
- 三条素材经真实入库/分析入口产生的系统结构化输出、模型和提示词版本；
- 高光 Top3、匹配 Top3 与证据时间码；
- direct-cut、A 转场词、B 60–100 秒连续解说的三个真实渲染；
- 入库、任务、队列、worker、QC、审核、导出与历史复播的真实记录 ID、日志和文件 hash。

因此事实/高光/匹配/转场/工程七维系统得分目前均为 `not_evaluable`，上线硬门禁维持 `NO-GO`。缺失证据不能记为通过，也不能记为系统失败。

## 对照轨启动条件

每个 case 至少提供：系统输出不可变快照、系统 commit、模型、提示词版本、真实记录 ID；匹配和转场评测还必须提供对应剧集及三种模式的真实成片。拿到后只允许对冻结 golden 做比较，不允许改写 v1；若发现 golden 自身错误，新建 v2 并保留 v1 和变更理由。

## 首批强制回归断言

1. QS-GH-002 在人物、演员或视觉连续时仍必须保持外搭困难正例，不得以“人物属于原剧”为否决条件。
2. 三条素材来源类型只能作为标签/诊断，不得成为钩子-正片匹配硬门禁。
3. 对应原剧/血缘缺失时，系统必须输出不确定或待复核，不得把视觉风格当成镜头来源事实。
4. 任何匹配都必须逐项证明人物/因果/叙事承诺由正片承接；仅题材或强冲突相似不得通过。
5. direct-cut、A、B 均须经过真实预览和人工批准；未批准资产不得渲染/导出。
