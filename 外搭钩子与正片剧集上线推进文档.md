# 外搭钩子与正片剧集上线推进文档

> 文档状态：执行中
> 建立日期：2026-08-30
> 当前阶段：上线收口与端到端验证
> 上线原则：本地开发、测试、构建和上线准备可持续推进；未经当前任务明确授权，不部署、不发布、不修改生产环境。

## 1. 文档用法

本文档是“外搭钩子＋正片剧集”上线工作的单一事实源。后续每次围绕该目标的对话都应更新本文档，至少记录：

- 本轮做出的产品或技术决策。
- 实际完成的代码、数据或配置变更。
- 测试命令、结果和可复现证据。
- 新发现的风险、阻塞项及责任归属。
- 下一个最小可执行动作。

状态只使用：`待开始`、`进行中`、`已完成`、`已阻塞`。“已完成”必须有验证证据，不以界面已存在或代码已编写作为完成标准。

## 2. 最终目标

将以下两条制作链路交付为可上线功能：

1. **正片剧集链路**：从本剧高光起点进入，保留当集剩余内容，按原剧顺序连接后续 2～3 集，生成可审核、可导出的成片。
2. **钩子＋正片链路**：从跑量素材库中媒体可用、边界已验证的钩子出发，匹配本剧承接段，组成钩子、过渡和正片时间线，生成可审核、可导出的成片。钩子可以是本剧原生、他剧、原剧人物资产复用型、AI 生成或混合型；来源是标签和诊断维度，不是准入硬门禁。

交付不得用模拟进度、模拟成片或伪造成功状态替代真实处理结果。

## 3. 上线完成定义（Definition of Done）

只有以下条件全部满足，功能才可标记为“可上线”：

### 3.1 共同门槛

- [x] 项目的 JavaScript/TypeScript 组件/契约测试全部通过（最新整合基线 103/103、0 fail、0 TODO；不代表真实数据完整业务 E2E）。
- [x] Python 组件、worker 与隔离队列测试全部通过（最新整合基线 132/132；夹具会直接准备数据库记录，不代表完整业务 E2E）。
- [x] 本地 production build 通过。
- [ ] 使用真实视频跑通项目保存、任务入队、worker 处理、预览、审核和导出。
- [ ] 失败、超时、片源缺失和质检不通过时，界面显示真实、可操作的错误。
- [ ] 成片质检至少覆盖：文件存在、H.264 视频、AAC 音轨、分辨率、时长一致、剪辑边界可追溯、集尾闪白避让。
- [ ] 输出文件带有可追溯的项目、渲染版本和文件哈希。
- [ ] 上线必需环境变量、依赖、worker 启动方式和故障恢复步骤已有文档。

### 3.2 正片剧集验收

- [x] 起播点必须来自已批准、边界已验证的真实高光资产（服务端契约已强制）。
- [x] 首集从高光起点播放至该集真实结尾（服务端使用剧集记录校验并持久化 canonical timeline）。
- [x] 后续只能连接 2～3 个连续集，且均从 0 秒开始（服务端契约已强制）。
- [x] 总时长为 5～15 分钟（保存与渲染双重校验）。
- [ ] 不得在用户未知情时使用不可用或越权集数。
- [ ] 至少完成 1 条真实成片的人工观看验收。

### 3.3 外搭钩子验收

- [ ] 不以 `source_class === external_material` 作为匹配或渲染硬门禁；只要已纳入跑量素材库、媒体可用且边界 `verified` 即可进入候选。
- [ ] 首轮质量门槛：故事完整度 ≥80、钩子留人 ≥80、叙事承诺兑现 ≥75、人物/因果/空间/情绪连通性 ≥70；阈值后续用黄金样本校准。
- [ ] 严重事实冲突或虚假叙事承诺一票否决；过度剧透必须进入风险诊断和人工审核。
- [ ] 外搭按四个子类记录：无关人物/场景、复用原剧人物资产但镜头新生成/新制作、混合型、来源不确定待复核。
- [ ] 最终来源结论必须由完整原剧源片镜头级比对和/或制作血缘支持；人物、场景、风格与 T1/T2 只能作视觉初判。
- [ ] 只允许媒体可用、归属可追溯且边界状态为 `verified` 的跑量素材库钩子进入生产，来源类型不作硬过滤。
- [ ] 钩子时长为 5～60 秒。
- [ ] 钩子、剧目和剧情匹配记录归属一致。
- [ ] 时间线至少包含钩子与一个真实正片片段。
- [ ] 所有正片剪辑边界被时码证据完整覆盖。
- [ ] 含付费集时必须获得明确确认后才能渲染。
- [ ] 过渡不得破坏人物、因果、空间或情绪的基本可理解性。
- [ ] 至少完成 1 条真实成片的人工观看验收。

### 3.4 钩子→正片过渡生产与审核

时间线必须持久化正式、版本化的 transition/narration production object，不能只保存 strategy 文本。最小字段：`type`、`gapDiagnosis`（time/space/character/causal/emotion）、`start/end`、`copy/script`、`language`、`voice/audio`、`evidence`、`renderConfig`、`reviewStatus`、`reviewerNote`、`version`。

- [ ] A「转场＋转场词」正式支持硬切、淡入淡出、黑场、闪白避让、证据支持的匹配硬切；根据断层诊断推荐方式。当前没有运动/构图自动匹配算法，禁止把 `match_cut` 对外描述为自动视觉匹配剪辑。
- [ ] A 的转场词可人工编辑，支持字幕、可选配音、语言/本地化、持续时长和安全区预览；“十年后/二十年前/与此同时/重生之后/穿越到现代”等时间或关系文本必须有剧情证据，证据不足时禁止生成事实性措辞。
- [ ] B「开头连续解说」从 0 秒连续覆盖约 60～100 秒的钩子、过渡和正片早段；脚本包含停滑句、必要背景、人物关系、因果桥接和正片事件推进，不得虚构剧情。
- [ ] B 支持人工编辑、语速/预计时长校验、TTS 或人工音轨、原声压低、关键原声保留和字幕；没有真实音轨时必须明确阻断，不能模拟已混音。
- [ ] 系统先诊断断层并推荐 A 或 B；直接拼接连通性很高时可推荐 `direct_cut`，但也必须人工确认。
- [ ] 渲染前提供真实媒体审核片：钩子结尾前 10 秒＋过渡＋正片开头 20 秒，支持修改、重新生成、批准、驳回；不得用静态占位或假进度代替真实渲染。
- [ ] transition/narration `reviewStatus !== approved` 时禁止最终渲染和导出；修改 copy/script/audio/renderConfig 后版本递增并使既有批准失效。
- [ ] 验收覆盖时间/因果正确、故事可理解、转场文字可读、解说时长匹配、音量平衡、无字幕/台词截断、过渡后留人不明显下降。

### 3.5 已批准的渲染技术路线（2026-08-30）

- 当前上线版本保留 FFmpeg/FFprobe 作为剪辑、拼接、音频、字幕、编码和 QC 底座，不重写主渲染链路。
- P0/P1 音频增强：旁白与最终音轨采用两遍 `loudnorm` 或具有同等可复现证据的标准响度处理；旁白区间采用 `sidechaincompress` 或经过真实媒体测试的动态 ducking；片段和过渡的音频边界使用 `acrossfade`，不得只做生硬音频拼接。
- 字幕升级为 ASS 或等价的版本化模板，至少支持安全区、字体、描边、可读换行和重点词样式；字幕渲染结果必须进入真实审核片。
- 建立 6～10 套按 time/space/character/causal/emotion 断层及直接连通场景选择的转场模板，模板只给出可编辑建议，事实性文字仍须剧情证据支持。
- `match_cut` 仅代表“证据支持的匹配硬切”。除非未来实现并验证运动/构图匹配算法，不得宣称自动视觉匹配剪辑。
- Remotion 列入第二阶段，只承担高级转场、动态字幕和视觉包装；最终媒体仍交由 FFmpeg 编码和 QC。本轮不引入全链路重写。
- 真实验收必须对同一批真实钩子/正片至少生成 A/B/C 三类版本：`direct_cut`、转场词、60～100 秒连续旁白；盲评故事理解、钩子留人、承诺兑现、过渡自然度、字幕可读性和音频质量。没有真实看片记录与成片 hash 前，不得宣称效果已达生产级。

## 4. 当前实现基线

### 4.1 已确认存在的能力

- `FactoryWorkspace` 已包含正片模式与外搭钩子模式的主要交互。
- PocketBase hooks 已实现项目保存、渲染入队、进度查询、审核和导出接口。
- Python worker 已包含 FFmpeg 剪辑、合并、集尾闪白检测和输出质检。
- 外搭钩子已有边界、归属、匹配门槛、付费集确认和人工复核约束。
- 正片剧集已有“高光剩余内容＋后续 2～3 集”的服务端约束。

### 4.2 当前已知问题

| ID | 优先级 | 问题 | 影响 | 状态 |
| --- | --- | --- | --- | --- |
| P0-01 | P0 | `tests/hook-factory-contract.test.mjs` 的重复剧情去重用例失败：预期 4 个方案，实际 10 个 | 候选方案重复，且全量测试无法通过 | 已完成；按策略模式＋语义路线去重 |
| P0-02 | P0 | 尚未记录两条链路的真实端到端成片证据 | 无法证明可上线 | 待开始 |
| P0-03 | P0 | JavaScript 契约测试首个红灯后，Python 测试与 production build 未继续执行 | 完整基线未知 | 已完成；Node 27/27、Python 104/104、build 通过 |
| P1-01 | P1 | 尚未形成上线环境检查表和 worker 故障恢复手册 | 运维风险 | 已完成；已新增运行手册与只读预检脚本 |
| P1-02 | P1 | 仓库存在 4 个未跟踪的 PocketBase 素材存储目录 | 可能造成误提交或本地数据混淆 | 待确认，不擅自删除 |
| P0-04 | P0 | 当前业务库没有运行中的 analysis/render worker | 真实业务分析与成片渲染尚未执行 | 进行中；已补 macOS/Linux 单 worker 启动器和多 worker 监护器 |
| P0-05 | P0 | 当前 PocketBase 无剧目、剧集、钩子、匹配、项目或渲染记录 | 暂无可用于两条链路的真实 E2E 数据 | 待准备数据 |
| P0-06 | P0 | `qwen3-vl-plus` 返回模型级 `AllocationQuota.FreeTierOnly`，但同一已授权 Key 的 `qwen-vl-max` 与 `qwen3-vl-flash` 可用 | 选错模型会阻断真实语义分析 | 已解除；主模型固定为质量优先的 `qwen-vl-max`，Flash 仅作降级/吞吐备选，不再调用 Plus；未充值或关闭费用保护 |
| P1-03 | P1 | 全仓库 ESLint 存在 875 个历史问题（736 errors / 139 warnings） | 无法将全仓 lint 作为当前发布门禁 | 进行中；本轮改动文件无新增 scoped lint error |
| P1-04 | P1 | `npm install` 初始报告 21 个依赖漏洞 | 需区分构建期与运行时可达风险 | 已完成；安全升级后全量剩 6 个 dev-only，`npm audit --omit=dev` 为 0 |
| P1-05 | P1 | 跟踪的 PocketBase seed 和两条媒体在本机仅为 Git LFS pointer | 新环境可能在无清晰错误时初始化空库 | 进行中；seed 已有完整性校验 hydration，媒体 LFS 仍待解决 |
| P0-07 | P0 | Hosted PocketBase 若公网可达，可绕过 UI 网关；部分 collection rules 仍公开 | 未认证 CRUD、审核或 worker 接口可能被直接调用 | 进行中；认证网关实现中，PB 私网化与 collection rules 锁定为上线硬门禁 |
| P0-08 | P0 | 素材入库允许服务端下载任意 URL，存在 SSRF/资源耗尽风险 | 可探测内网或消耗服务资源 | 已完成；公网 IP/DNS/逐跳重定向复验、超时和 250MiB 双重上限，离线测试 7/7 |
| P0-09 | P0 | 锁定 collection rules 后，Next gateway 的共享 Bearer 不能访问 PocketBase 内置 collection API | 直接上线规则迁移会令 Hosted UI collection 读写全部 403 | 已完成；13 个 collection rules 锁定，gateway 精确白名单使用服务端短期 PB JWT，匿名 403、gateway 200 |
| P0-10 | P0 | Fresh PocketBase 全量迁移在 `1787569400_backfill_drama_ontology_tags.js` 出现 `Invalid module` | 新环境无法可靠初始化 | 已完成；空库不加载运行时 hook，fresh 全量迁移通过；非空历史回填失败时由 reanalysis/reprojection 补齐 |
| P0-11 | P0 | 钩子→正片仅有过渡策略文本，没有正式生产对象、真实预览和独立人工审核 | 成片可能出现时间/因果错误、突兀硬切、台词截断或伪造转场词 | 已完成最小技术闭环；A/B/direct_cut、真实审核片与 hash/version 审批门禁已实现，真实业务盲评仍待完成 |
| P0-12 | P0 | 外部数据 API key 曾按项目现有 HTTP endpoint 发起只读请求 | 明文传输可能泄露凭据 | 待轮换；已停止外部调用，HTTPS/受信隧道与新 key 就绪前禁用接口 |
| P0-13 | P0 | B 模式仍缺真实剧目/真实钩子的完整业务验收与盲评 | 受控上传→真实 PB 队列/worker/FFmpeg 审核片→批准→最终渲染的隔离 E2E 已完成，assetId/hash/version 一致；17 项上传/替换/删除/超限验收全通过 | 技术组件已完成；等待真实数据完整链和盲评，禁止把隔离合成媒体 E2E 描述为生产效果通过 |
| P0-13 | P0 | 尚未用真实 UI/API/worker 跑通从导入、分析、灵感、匹配、过渡到历史复播的完整业务链 | 组件/隔离测试可能掩盖入口、状态回写、内容质量和跨页面集成缺陷 | 待执行；使用功能链条验收矩阵逐站留证，禁止最终验收直接写库或夹具绕过生产入口 |

## 5. 执行路线图

| 里程碑 | 交付物 | 完成标准 | 状态 |
| --- | --- | --- | --- |
| M0 基线固定 | 上线文档、红灯清单、完整测试结果 | 所有现存失败可复现且已分类 | 已完成 |
| M1 契约稳定 | 方案生成与时间线契约修复 | 相关 JS/Python 测试全绿 | 已完成 |
| M2 正片闭环 | 真实正片剧集成片 | 端到端渲染、质检、审核、导出通过 | 进行中；合成媒体队列 E2E 已通过，真实业务片源待验收 |
| M3 外搭闭环 | 真实外搭钩子成片 | 端到端渲染、质检、审核、导出通过 | 进行中；合成媒体队列 E2E 已通过，真实语义与业务素材待验收 |
| M3.1 过渡闭环 | 正式 transition/narration 对象、真实预览、人工审核、渲染 | A/B 两模式与 direct_cut 均有证据、版本和 approved 门禁 | 进行中 |
| M4 异常与恢复 | 错误态、重试、运维手册 | 主要失败场景可观测、可恢复 | 进行中 |
| M5 上线准备 | 验收报告和发布清单 | Definition of Done 全部满足 | 待开始 |
| M6 生产发布 | 部署与发布记录 | 用户在当前任务明确授权后单独执行 | 待授权 |

## 6. 当前迭代

### Iteration 01：基线与首个红灯

**本轮目标**

1. 建立上线推进文档。
2. 修复重复剧情候选方案过多的契约回归。
3. 完整执行 JavaScript 契约测试、Python 测试和 production build。
4. 根据全量结果更新 M0/M1 状态。

**当前下一步**

准备可用的 FFmpeg/FFprobe、worker 配置和真实剧目/剧集/高光数据，先跑通正片剧集成片闭环。

### Iteration 02：真实渲染环境与正片闭环

**本轮目标**

1. 安装或为项目提供可复现的 FFmpeg/FFprobe。
2. 建立可运行的 macOS/Linux worker 启动与监护方式。
3. 准备一部含连续 3～4 集真实片源、且存在已批准高光的验收剧目。
4. 跑通正片项目保存、渲染入队、worker 合成、质检、人工审核和导出。
5. 将成片文件、哈希、质检报告和观看结论记入本文档。

## 7. 决策日志

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-08-30 | 以本文档作为后续对话和开发的单一事实源 | 避免功能、验证和上线状态分散 |
| 2026-08-30 | 先修契约红灯，再做真实端到端成片 | 在可重复基线上定位端到端问题 |
| 2026-08-30 | 先验证正片剧集链路，再验证外搭钩子链路 | 正片链路的输入和时间线更简单，适合先确认渲染基础设施 |
| 2026-08-30 | 导出必须以真实预览和人工审核通过为前置条件 | 防止将仅有任务状态或未审核成片误交付 |
| 2026-08-30 | 正片拼接的安全边界由服务端回查真实资产并重建，不信任前端的 `verified` 声明 | 防止未审批高光或伪造安全边界进入渲染 |
| 2026-08-30 | 渲染输出先在临时工作区质检，通过后原子发布 | 质检失败时不得留下可下载的伪成功文件 |

## 8. 进展日志

### 2026-08-30 · 对话 01

- 新增本上线推进文档。
- 完成仓库结构、内容工厂界面、PocketBase hooks、渲染 worker 和契约测试的初步盘点。
- 确认功能已有较完整实现雏形，当前重点是端到端收口，不是从零开发。
- 执行 `node --test tests/hook-factory-contract.test.mjs`：12 个用例中 11 个通过、1 个失败。
- 失败用例：`removes repeated plot clauses and does not return duplicate storyline content`，实际方案数为 10，预期为 4。
- 因命令使用 `&&` 串行，首个测试失败后 Python 测试和 production build 未执行，已记为基线待办。
- 未修改或删除 4 个未跟踪的 `pb_data/storage` 素材目录。

### 2026-08-30 · 对话 02

- 修复故事线候选去重回归：按“策略模式＋语义剧情路线”去重，同一剧情不再因时间码或集数不同重复膨胀，四种制作策略仍独立保留。
- 修复 Python 单测对本机可选 `faster-whisper` 安装的隐式依赖，无重型 ASR 依赖的开发机也能可重复测试无音轨逻辑。
- 补回成片人工审核 UI，导出现在要求真实预览且审核通过；批量版本逐个记录审核状态。
- 修复单任务/批量渲染轮询在一次网络错误后永久停止、保存草稿重复持久化竞态、渲染配置固定 9:16 三个问题。
- 渲染成片改为临时目录生成、质检通过后再原子发布；输出文件名包含 render ID，避免同标题同版本覆盖。
- 正片拼接服务端现在回查已批准且边界已验证的 `episode_highlight` 资产，持久化 canonical timeline；渲染器不再自动补造 `verified` 边界。
- 为剧集分析、钩子匹配、接点精校、补充高光和成片渲染队列增加持久化错误分类与重试退避；临时 PocketBase 实例已成功应用迁移并确认 5 个集合字段存在。
- 新增 `docs/外搭钩子与正片剧集运行手册.md`、`scripts/runtime-check.sh` 和 `npm run runtime:check`。
- 主任务整合验证：`python3 -m unittest discover -s tests` 104/104 通过；`npm run build` 通过；`node --test tests/*.test.mjs` 27/27 通过；`git diff --check` 通过。
- 首次运行预检结果：PocketBase 健康、Node 版本合格；发现 7 个失败和 2 个警告。随后引入 npm 锁定的 `ffmpeg-static@5.3.0` 和 `@ffprobe-installer/ffprobe@2.1.2`，Python 处理器与预检脚本均可解析项目内二进制；失败降为 5 个，仍缺 worker token、模型 Key 和 Python 分析依赖。
- 当前 PocketBase 数据库中无剧目/剧集/钩子/匹配/项目/渲染记录，暂无法产生两条真实 E2E 成片。

### 2026-08-30 · 对话 03

- 建立隔离媒体级 E2E：用项目内 FFmpeg 生成真实 H.264/AAC MP4，通过临时 HTTP 文件服务走真实下载、剪辑、拼接、QC、边界台账、SHA-256 和原子发布路径。
- `npm run test:media-e2e` 中 episode-splice 与 external-hook 两条媒体路径 2/2 通过，且全部在临时目录运行，未访问 `pb_data`。
- 渲染器新增仅代码调用方可注入的测试约束；API payload 不能改写生产默认的 1080×1920 和 300～900 秒限制。
- 新增 macOS/Linux 单 worker 启动器和多 worker 监护器，支持队列选择、实例数、指定任务、信号清理和异常重启；PocketBase 与 worker token 不一致时拒绝启动。
- 依赖安全升级后，漏洞从 21 个降至 6 个 dev-only；`npm audit --omit=dev` 为 0，未使用 `--force`，未接受破坏性降级或 beta major 升级。
- 只读验收数据审计确认：当前库仅有 4 条可播放外部广告片段，可作外搭候选，不是连续剧集；不得将它们伪装成正片验收数据。
- 确认 `pb_data/data.seed.db` 是 Git LFS pointer；主任务已在临时目录成功下载并校验真实 seed（SHA-256 匹配，SQLite 包含 1 部剧和 4 条剧集记录）。
- 新增 `scripts/hydrate-pocketbase-seed.sh`：通过 Git LFS batch API 获取 seed，验证 OID、字节数和 SQLite header，且拒绝覆盖已存在的数据库；`start-pocketbase.sh` 仅在 `data.db` 不存在时触发。
- 媒体 LFS 对象尚未复原，且 seed 内 4 条剧集记录只有 1 条对应的跟踪媒体 pointer，因此仍不足以执行真实业务剧目验收。
- 已启动下一波并行工作：隔离 PocketBase + worker 正片队列 E2E、外搭队列 E2E，以及按 render/analysis/full 拆分的运行预检。
- 正片剧集隔离队列组件测试已通过：在随机端口和临时 PocketBase 中直接写入合成剧目/剧集/高光夹具，再覆盖项目 API、worker、QC、审核和导出。它只证明这些下游组件能协作，不包含真实导入、分析、剧库 UI、内容质量或历史复播，不能称为完整业务闭环。
- 正片 E2E 发现导出 API 会在未人工审核时自动批准的 P0；已移除自动审批，现在必须已有 `approved` review，且 render ID、版本和输出 SHA-256 与当前成片精确一致。测试已覆盖审核前导出被拒绝。
- 钩子＋正片隔离队列组件测试已通过：合成素材、钩子、match 等前置记录由夹具直接写库，再验证项目、worker、FFmpeg、审核与导出。它没有覆盖真实素材采集/入库、灵感大屏、分析、匹配质量、真实 UI 操作或我的创作，不能称为完整业务闭环。
- 外搭 E2E 发现 sequential body 的 worker claim 只根据 match 首集锚点下发片源，不会下发时间线中后续 2 集的 P0；已改为 `sequential_from_highlight` 从项目时间线收集全部集数，并被真实 worker E2E 覆盖。
- 运行预检已拆分为 `render|analysis|full` 三种 profile。主任务使用临时非敏感 token 实跑 render profile：0 个失败、0 个警告；分析 profile 仍严格要求模型凭据、JSON Schema、ASR 和 OCR。
- 新增通用 `scripts/hydrate-git-lfs-file.sh`，仅允许显式 pointer 和新目标，拒绝覆盖，校验 HTTPS origin/download、OID 和 size；独立契约 4/4 通过。
- 新增只读队列观测脚本，覆盖 6 类 worker 队列的 queued/active/failed、lease 过期、backoff 到期和 attempt 耗尽；使用 SQLite `mode=ro&immutable=1`，不读取 token、不修改任务状态。
- 新增 `docs/外搭钩子与正片剧集上线验收清单.md`，每项包含 P0/P1/授权门禁、负责人、证据位置和复核命令；未证实的真实业务验收、监控、备份恢复和生产授权项仍保持未勾选。
- 最新主任务整合回归：Python 108/108（含两条队列 E2E，约 40 秒）、Node 40/40、Vite/vinext production build 通过、`npm audit --omit=dev` 0 漏洞、`git diff --check` 通过。

### 2026-08-30 · 对话 04

- 产品定义纠正：外搭不以“人物是否属于原剧”或“人物是否变化”为条件。复用原剧人物/角色资产、但镜头在正片之外新生成或新制作的开场仍是外搭。
- 新增四类外搭子类：无关人物/场景外搭、复用原剧人物资产但镜头为新生成/新制作、混合型、来源不确定待复核；已写入语义契约与分析输出 `creative.externalHookSubtype`。
- 收到 3 条黄金外搭正例；第二条指定为“视觉与人物连续但仍为外搭”的困难正例。正在生成媒体指纹与镜头基线；没有对应完整原剧源片前只记录黄金标签与待复核来源，不能用 T1/T2 视觉初判冒充镜头血缘结论。
- 安全审查发现 hosted 身份边界与 PocketBase 旁路风险：托管环境必须禁用 local-loopback，PocketBase 不得公网可达，公开 collection rules 必须锁定，UI 网关需使用 method+exact-path 白名单、CSRF 和隔离的 server-only token。该项列为 P0 上线硬门禁。
- 素材入库 SSRF/DoS 已修复：仅 HTTP(S)，DNS 全结果和逐跳重定向公网校验，固定已校验 IP 防重绑定，10 秒连接/120 秒总超时，Content-Length 与流式 250MiB 双重上限；离线安全测试 7/7、build 通过。
- 本机 UI 授权收紧后的隔离队列测试首次复跑因 PocketBase `requestInfo` 不暴露自定义头而 403；改用底层 request header 读取后组件路径通过，未放宽 hosted 边界。该结果不包含真实浏览器身份与完整业务入口验收。
- 产品门禁调整：取消匹配/生产/渲染阶段“必须为 `external_material`”的来源限制。来源兼容性改为评分/诊断，不再硬过滤；核心目标改为故事完整、低理解成本、留人、承诺兑现和过渡可理解，严重事实冲突/虚假承诺一票否决。
- 来源门禁调整已贯穿前端、PocketBase、分析 worker 与 FFmpeg 渲染器：现在要求跑量素材关联、可播放媒体、有效区间、`verified` 边界、人工 `approved`、权限/付费确认；来源类别仅进入 `ranking_diagnostic`。留人/连通性分数不存在时明确标记 missing，不伪造分值。
- 三条黄金样本基线已生成至 `artifacts/golden-hook-samples/`，包括 SHA-256、FFprobe、场景边界、CSV 和抽帧联系表。时长分别为 1184.244、1223.927、1003.290 秒，均为 720×1280、25fps、H.264/AAC；第二条固定为资产复用新镜头困难正例。缺少原剧源片/制作血缘，因此最终来源仍统一标为“来源不确定待复核”。
- Hosted PocketBase gateway 已完成：生产浏览器不再获得 PB URL/token，强制 ChatGPT 身份、method+exact-path 白名单、same-origin CSRF、内部头剥离和 server-only Bearer；伪造 local header 的真实请求返回 401。部署前仍需确认托管边缘剥离伪造身份头、PB 私网不可直达并锁定 collection rules。
- 最新整合 `npm test` 全绿：production build、Node 70/70、Python 121/121、`npm audit --omit=dev` 0、`git diff --check` 通过。所有这些均归类为组件/契约/隔离技术链证据，完整业务链与内容质量仍是 P0 未通过。
- 上线失败注入 6/6 通过并固化到 `release-evidence/2026-08-30/failure-injection/`：源 404、坏媒体、无音轨、时长/编码不符、transient/permanent 重试分流、QC 失败不发布；未发现需要修改生产实现的问题。
- 数据盘点已自动识别并否决本地 `TheGodfathersObsession-01..10.mp4` 作为连续剧集候选；这不是唯一数据入口。现有 `GET /api/external-data/playback?name=完整剧名` 契约可返回 `items[].total_episodes`、完整 `episodes[{episode,url,type}]` 和有效期，前端已有“全集加入剧库”，脚本可下载入库。
- 数据盘点证据已写入 `release-evidence/2026-08-30/data-inventory.{md,json}`：10 个本地候选均可解码、720×1280 H.264/AAC、总约 101.52 分钟并已记录 SHA-256，但仍需内容连续性与验收权限核验；40 条跑量元数据覆盖 8 题材各 5 条，7 类有月榜标签，婚恋背叛标签与全部来源子类仍待看片冻结。
- 外部接口盘点曾使用仓库现有 HTTP endpoint 发出带 key 的只读请求；证据文件未保存 key。用户后来明确授权仅在本地验收阶段继续使用该 HTTP 上游，因此验收拉取可执行；生产代码的 HTTPS/loopback fail-closed 保护保持不变，HTTP 地址仍不得进入生产配置。
- 过渡第一阶段已落地正式 production object、独立 transition-review API、版本/证据校验，以及 API 入队与 worker 双重 `approved` 门禁；`direct_cut` 同样需要人工确认，编辑会使批准失效。
- A「转场＋转场词」已进入真实 FFmpeg 路径：生成实际转场卡与 AAC 音轨后插入钩子/正片之间，支持规定转场枚举；时间/因果措辞无 evidence 时服务端拒绝。相关媒体与失败注入 10/10、Hook Factory 18/18 通过。
- 前端已提供 A/B/direct_cut 正式对象编辑与审核界面，只在后端返回真实 `reviewPreviewUrl` 时播放，缺失时明确不可批准，不会伪造预览；最终/批量渲染均检查过渡批准状态。
- 第一阶段复核曾保持 P0 红灯：当时 B 真实音轨混音与审核片 hash/version 绑定尚缺；第二阶段已补齐，避免把仅有 UI/对象声明误报为完成。
- 过渡第二阶段完成：B 已支持真实人工音轨下载、60～100 秒与音轨误差校验、原声压低、关键原声窗口保留、真实字幕烧录和 FFmpeg 混音；无音轨明确失败，TTS 仍可作为后续供应方能力。
- 真实审核片使用现有 render 队列生成钩子末最多 10 秒＋真实过渡＋正片开头最多 20 秒，返回 URL/hash/preview version/production version；审批严格匹配四项，编辑会清空旧证据。Hosted gateway 已补充 transition-preview/review 的 method+exact-path 白名单。
- 连续前 10 集候选视觉抽检结论已写入 `release-evidence/2026-08-30/episode-continuity-review.md`：本地 10 条更像跑量广告/混剪，禁止冻结。真实剧集仍有两个入口：A）轮换 key 并取得 HTTPS 地址/受信隧道后，优先走月榜→完整剧名→playback 全集→在 `expires_at` 前落盘校验→只分析前 10 集；B）用户提供已授权本地连续前 10 集。
- 已建立 `release-evidence/2026-08-30/business-chain-matrix.{md,csv}`：8 站主链＋9 项异常共 17 行、12 个证据字段。当前结论为 `NO-GO`；隔离队列/合成夹具/失败注入只标 `component_passed`，业务链路和内容质量均保持待通过。
- A 模式效果真实性第一阶段已收口：`hard_cut`、`fade`、`black`、`flash_avoidance` 分别进入真实且可区分的 FFmpeg 路径；内部枚举 `match_cut` 当前只是“证据支持的匹配硬切”，缺少出入镜头特征证据时拒绝渲染，不具备自动运动/构图匹配能力。真实媒体效果/像素/哈希与负向测试 8/8 通过；6～10 套断层模板仍在实施。
- B 模式新增生产安全/QC：人工音轨只接受与渲染媒体源同源的受控 URL，逐跳校验重定向并设 100MiB/120 秒上限；音轨须真实满足 60～100 秒且非静音，关键原声窗口有范围/排序/重叠约束；字幕按句拆分并固定安全区。真实 FFmpeg 回归 13/13 通过。UI 受控上传入口仍为 P0 缺口，故不得表述为业务闭环。
- fresh PocketBase＋实际构建网关隔离验证已通过：匿名 collection 为 403，模拟可信 ChatGPT 身份可经网关访问，transition 路由可到达 PB，worker claim 保持可用；Node 全套当次 72/72。目标预发边缘是否剥离客户端伪造的 `oai-authenticated-*` 仍须在真实托管环境验证。
- 本地浏览器 UI 冒烟已覆盖剧库、内容工厂、我的创作、数据源和任务中心；清除了侧栏假计数/假同步时间以及固定成本、SLA、队列等待时间，未接入的数据明确显示“未接入”。这只属于界面真实性组件检查，不计完整业务 E2E。
- 外部接口服务端已 fail-closed：未配置 base URL 时拒绝，远程仅允许 HTTPS，HTTP 只允许 loopback，保证远程明文地址在发送 API key 前失败；构建与定向安全测试通过。
- B 模式任意音轨 URL 已从 UI 移除，改为项目级 multipart 受控上传：浏览器校验格式/100MiB/可解码/60～100 秒并计算 SHA-256；服务端重新以 FFprobe 探测容器、音轨和时长，并重算 SHA-256。资产绑定项目、上传人、状态和 probe evidence，worker 只通过项目＋asset＋hash 绑定的 HMAC 同源地址下载；通用 `/api/files` 访问旁白资产为 403。fresh PocketBase＋实际网关已验证真实 WAV 上传/下载 hash 一致，以及未认证 401、跨站 403、伪 hash/格式/时长 400。
- 渲染技术路线按正式决策增强但不重写：旁白两遍 loudnorm 至 -16 LUFS，最终成片两遍 loudnorm 至 -14 LUFS/TP -1.5 dB；旁白以 sidechaincompress 动态压低原声，关键原声窗口恢复原声并降低旁白；真实音频边界使用 25ms acrossfade；QC 新增标准响度和音画时长同步。
- 连续解说字幕已升级为正式 ASS 渲染，支持字体、字号、颜色、描边、阴影、粗体、对齐、安全边距和 2/3 行限制。前端提供 6 套字幕样式和 8 套按断层类型推荐的转场模板；模板不会自动填入事实性剧情文字。真实媒体测试已验证响度收敛、动态 ducking、交叠频率和 ASS 像素输出，但真实素材盲评仍待执行。
- B 音轨及过渡门禁独立验收 18/18 通过、0 TODO：真实项目级上传→PB 队列→worker/FFmpeg 审核片→API 批准→最终渲染保持相同 assetId/hash/transitionVersion；替换/软删除会服务端即时清除 preview/hash/approval、递增版本并让旧签名媒体 401；排队/渲染中禁止变更；显式超限为 413，缺 Content-Length 为 411，拒绝后记录和存储快照均无孤儿/partial；A pending、B rejected、direct_cut draft 及 B 音轨变更后的旧批准均不能最终渲染或导出。以上仍是隔离合成媒体技术 E2E，不等于真实内容盲评。
- B 隔离 E2E 暴露并修复真实音频缺陷：60 秒旁白结束后 sidechain 控制流曾使 5 分钟最终音轨提前终止；现控制流补齐至 source duration，旁白结束后原声持续，`AUDIO_VIDEO_SYNC` 通过。
- 验收媒体 URL 健康检查统一使用 `GET`＋`Range: bytes=0-0`，接受对象存储正常的 HTTP 206；不得使用 `HEAD`，因为该对象存储对 HEAD 返回 403，不能据此判定素材失效。
- 语义模型凭据状态已更正：千问 Key 的鉴权格式有效；`qwen3-vl-plus` 的最小真实调用返回模型级 `AllocationQuota.FreeTierOnly`，并非 Key 无效。同一已授权 Key 对 `qwen-vl-max` 和 `qwen3-vl-flash` 的最小调用均成功；按质量优先决策，主分析模型使用 `qwen-vl-max`，Flash 只作为明确降级/吞吐备选，不再调用 Plus。没有充值或关闭费用保护。
- 2026-08-30 最新整合回归再次通过：本地 production build、Node 103/103（0 fail、0 TODO）与 Python 132/132；`git diff --check` 通过。该结论仍仅表示组件/契约/隔离队列链路通过，不替代真实 UI/API/worker 完整业务 E2E 与内容盲评。
- 运行预检已与 PocketBase 启动器的安全默认值对齐：项目固定 FFprobe、系统 `sha256sum`/`shasum`、本机 worker 媒体地址及首次启动安全生成 worker token 不再被误报；并改为检查 worker 实际使用的 `LUMINA_PYTHON_EXE`/项目 venv，而不是误查 shell Python。契约测试 6/6 通过。当前 `full` 预检从 8 个失败归并为 2 个真实门禁：缺少语义模型凭据、当前 Python 3.14 不兼容分析栈（要求 3.9～3.12）；另有 PocketBase 未启动、外部 API Key/生产 HTTPS 尚未配置 3 个警告。不得把真实依赖缺口改成 warning 来“清零”。
- 任务中心已停止按百分比伪造六段完成轨迹，改为透传并显示服务端真实 `current_stage`；失败原因、`error_kind`、`next_attempt_at` 和实际 attempt/max_attempts 常驻展示。修复了剧集分析 job 的 retry 路由错误使用 worker-only 鉴权、导致 UI 永远无法调用的问题；现在只有失败的剧集分析任务可经服务端安全重新入队，其他尚无已验证 retry API 的任务类型继续禁用并明确说明。production build、页面契约 6/6、预检/重试契约 9/9 通过；渲染 QC 结构化错误、卡死/lease 提示及其他任务类型服务端重试仍是 P0/P1 缺口。
- 已完成当前 36/36 个 PocketBase migration 的前向/回滚兼容性审计，证据见 `release-evidence/2026-08-30/migration-audit.md`。结论为 NO-GO：6 个数据迁移不可逆，多个 down 有枚举/required/匿名权限/文件删除/审计血缘丢失风险；上线必须先做真实数据副本升级和数据库＋媒体一致备份恢复演练，默认走 forward-fix，不承诺一键无损 down。
- 剧库异常链已补齐：PB 读取失败时缓存明确标为非实时并常驻显示同步错误/最后成功时间；分析重试 rejection 被捕获；播放失败常驻展示资产 ID、可能原因和真正重建播放器的重试动作。渲染交付页展示真实 task ID、QC failureCodes/technical evidence 和分类处置建议；根据服务端 `created/updated/lease_until` 只提示“疑似卡住”，不由前端擅自判失败；已移除本地递增演示进度，并把“内容规则检查”与真实媒体 QC 明确分开。相关定向契约 10/10 与 build 通过。
- 外部 playback 导入新增“前10集验收入库”真实入口：按集号排序并强制 EP1～EP10 连续；每集先用 `GET Range: bytes=0-0` 验证 HTTP 206 和 `video/mp4`，绝不使用 HEAD；只把用户授权的内部验收前10集记入可分析范围，完整全集导入仍保持授权待确认。生产 HTTPS 门禁未修改。
- 三条用户黄金样片已冻结于 `artifacts/golden-hook-samples/manifest.json`：逐条 SHA-256、字节数、时长、H.264/AAC、720×1280、25fps 指纹均从原文件复核通过，未复制约 652MiB 视频。三条文件实际为约 16.7～20.4 分钟的完整成片，钩子 5～60 秒边界仍须盲标；第二条固定为人物资产复用型困难正例。
- 外部剧集数据接口与语义模型是两套独立凭据/链路：榜首剧 playback 已验证可返回 dramawave 全集，EP1～EP10 的签名媒体经 `GET Range: bytes=0-0` 均为 206/`video/mp4`；语义分析当前仅受千问额度策略阻断。不得再把该状态表述为 Key 无效，也不得用语义模型阻断否定已经完成的媒体可用性验证。
- 已使用 Codex 隔离运行时的 Python 3.12.13 创建 `.runtime/analysis-venv`，真实导入 `faster-whisper`、`jsonschema`、`paddleocr`、`paddle` 全部成功；指定该解释器后 `full` runtime check 只剩语义模型凭据 1 个失败及 3 个非生产警告。未改系统 Python，未创建或读取模型凭据。
- 独立监督 Agent 首轮资产已落盘：`release-evidence/quality-supervision/` 包含双轨协议、JSON Schema、评分表、差距模板、24 列 case 台账和 baseline-v1；`artifacts/quality-golden/QS-GH-001..003/` 包含三条样片冻结 golden、证据图和 SHA sidecar。主线复核结果为 3/3 schema VALID、3/3 hash 一致、台账 3 case×24 列；由于无对应原剧、系统输出和三模式成片，所有系统得分保持 `not_evaluable`，结论 NO-GO。
- 监督盲性复核发现 v1 冻结前曾读取含 `candidateSubtype/candidateConfidence` 的 legacy `samples.json`，因此三份 v1 已诚实降级为 `frozen_with_prior`，不得进入严格盲评分母；台账新增榜首剧 EP1～10 `pending_input` case（不伪造 hash/golden/分数）。已启动 fork_turns=none 的隔离评测者重做 v2，禁止读取 v1、legacy samples、系统输出和推进文档。
- 四类人工安全重试完成技术闭环：hook match、supplemental、entry precision 使用 job-id API；reason、idempotency key、expected status/updated 必填；活动任务 409 且保留 lease；permanent/media/validation 需显式 override；旧错误/退避/结果/worker/lease 在事务中清理并写 audit。render 重试不篡改失败记录，而是创建带 `retry_of` 的新版本并清空旧审批。任务中心和内容工厂已切换新契约，定向集成 22/22 通过；真实业务失败 case 仍待执行。
- 启动本地 PocketBase 前已生成数据库＋媒体同一停机时点备份，hash 见 `release-evidence/2026-08-30/pre-local-runtime-backup/README.md`。首次启动暴露并修复 macOS `find -perm` 导致 FFprobe 假缺失；随后本机 PB 健康并应用最新迁移。队列观察器又暴露 `immutable=1` 忽略 live WAL、误报 schema 缺列的问题，已改为 WAL-aware `mode=ro`；真实只读快照显示 6 类队列可观测、无过期 lease/到期重试/耗尽任务，现有 4 个素材分析任务正常 queued。

## 9. 独立功能验收方案（Codex 负责执行与监督）

实现测试通过不等于功能验收通过。开发者可以提供修复与证据，但黄金标签冻结、盲评、指标计算和最终验收结论由独立验收流程产生；在查看系统输出前不得修改黄金答案。

### 9.1 固定基础数据集

- 冻结 1 部具备连续前 10 集真实、可播放且有验收权限片源的剧，完成入库、分析并记录数据版本、每集 SHA-256、时长和权限范围。
- 冻结约 40 条真实跑量视频，建议分层：10 条异源外搭、10 条复用原剧人物资产的外搭、8 条原生正片钩子、6 条混合型、6 条边界模糊/负例。
- 约 40 条素材必须跨题材分层，不能集中于豪门/身份反转。目标覆盖至少 8 类、每类 4～6 条：豪门/隐藏身份、婚恋背叛、复仇逆袭、家庭伦理/亲子、狼人/吸血鬼/奇幻、穿越/重生、职场/契约婚姻、犯罪/悬疑/救援；若真实分布不同，报告保留真实占比，但每类仍至少 3 条，并记录补样方式。
- 每个题材内部尽量交叉覆盖异源外搭、人物资产复用外搭、原生钩子与混合型；每条保留跑量证据等级及来源时间窗，避免把“进入素材库”误当作同等强度的效果证明。
- 来源类型只作分层评测标签，不限制参与匹配。每条样本必须有稳定 ID、源文件哈希、媒体信息、授权/验收用途、黄金标签版本和冻结时间。
- 当前 3 条已提供黄金正例进入候选池，其中第二条固定为“复用原剧人物资产但镜头新生产”的困难正例；只有补齐对应原剧源片镜头比对/制作血缘后，才冻结最终来源归因。

### 9.2 盲标与冻结

至少两阶段执行：先由人工在不查看系统输出的条件下标注并冻结，再运行系统并比较。黄金字段包括：素材来源子类、T1/T2、钩子事件、人物关系、冲突、情绪、悬念、叙事承诺、留人评分、对应正片事件、高光起止、安全边界、是否可拼接。冻结包必须包含标注人、时间、版本、分歧处理记录和不可变哈希；系统输出不得回写黄金标签。

### 9.3 三项核心质量门

| 门 | 验收目标 | 暂定阈值 |
| --- | --- | --- |
| A 灵感大屏/素材分析 | 外搭判定正确，分析完整，所有关键结论可回到时码/画面/台词证据 | 类型准确率 ≥95%；人物资产复用型外搭召回 ≥90%；关键字段完整率 ≥95%；严重事实错误 ≤2% |
| B 前 10 集剧集分析 | 剧情、人物、关系和因果准确；每集 Top3 高光至少有 1 条可直接制作且边界安全 | 剧情事实认可 ≥90%；Top3 高光可用率 ≥90%；安全边界通过率 ≥95% |
| C 钩子与正片匹配 | Top3 至少 1 条高质量方案，不限制钩子来源；排序面向故事完整、留人、承诺兑现和连通性 | Top3 命中可用方案 ≥90%；故事完整 ≥80；留人 ≥80；承诺兑现 ≥75；连通性 ≥70；严重错配/虚假承诺 = 0 |

阈值是首轮验收门槛，必须用冻结黄金样本校准并版本化；任何调整都要保留调整前后的指标和理由，不得为使当前输出过线而静默改标。

除总体指标外，报告必须逐题材输出：题材识别、外搭判定、T1/T2、分析完整率、Top3 匹配命中、故事完整度、留人评分、承诺兑现和连通性，并同时给出分母与置信区间/样本数提示，防止总体准确率掩盖单一题材失效。

匹配评测另设跨题材集合：

- 负例：题材不同且人物/事实/承诺不可承接，即使都具有强冲突或身份反转，也必须拒绝或排出 Top3。
- 正例：保留少量题材不同但钩子结构、信息缺口与正文兑现关系真实可迁移的方案，验证系统不会把题材差异本身当作硬门禁。
- 每条跨题材样本必须记录为何可迁移/不可迁移、事实冲突、虚假承诺、剧透风险和人工结论。

### 9.4 真实成片与盲评

- 至少完成 3 条不同钩子的真实拼接成片，其中至少 1 条使用人物资产复用困难样本。
- 至少完成 1 条正片连续剧集成片。
- 盲评采用 1～5 分，平均分必须 ≥4/5；评审看不到实现版本、模型或开发者预期。
- 每条逐项检查：技术 QC、黑帧/花屏、音画同步、字幕/台词截断、音量跳变、集尾闪白、异常跳切、结尾卡点、内容权限、预览与导出哈希一致。

### 9.5 失败台账与复测纪律

每个失败必须记录：样本 ID/版本、预期、实际、失败时间码、指标影响、根因、修复提交或变更标识、复测命令、复测证据和结论。开发实现状态与独立验收结论使用不同字段；没有冻结数据、盲评或复测证据时不得标记验收通过。

当前执行顺序：按用户授权，在本地验收阶段可通过现有 HTTP 上游执行月榜→剧名→playback→前10集入库，但生产代码仍强制 HTTPS/loopback；也接受已授权本地连续前10集作为替代入口。临时播放 URL 必须在 `expires_at` 前落盘并逐集以 Range GET 校验，不得依赖过期远程地址。所有验收均为本地/隔离环境，生产部署仍需用户在当前任务中明确授权。

### 9.6 完整业务功能链条矩阵（P0）

最终验收必须使用真实浏览器 UI、对外 API 与实际 worker 入口；不得直接创建/修改数据库记录，不得用 synthetic fixture 预置已分析、已批准或已匹配状态。每一站必须记录真实输入 ID、UI/API 动作、数据库状态变化、返回内容质量人工抽检、截图/日志、文件 SHA-256、人工结论和缺陷 ID。

| 站点 | 必须完成的真实动作 | 内容/状态质量检查 | 当前结论 |
| --- | --- | --- | --- |
| 1 数据源→剧库 | 优先从安全接口按完整剧名取得 playback 全集并及时落盘，或导入已授权本地剧目；在剧库列表、详情和分集播放器查看 | 集号、时长、标题、媒体、权限、URL 有效期与播放连续性正确 | 部分通过：真实 UI 已完成 playback→EP1–10 落盘→列表/详情→EP1 播放；10 集 hash/时长已冻结，逐集人工播放质检仍待完成 |
| 2 前 10 集分析 | 从 UI/API 创建粗/细/高光任务；实际 worker 处理；观察进度、失败、重试、结果回写 | 状态机真实，失败可操作，结果版本可追踪 | 进行中：Golden 冻结后由真实 UI 创建 10 个粗解析任务，2 个 drama worker 已并行 claim；细解析/精解析/失败重试和质量复核待完成 |
| 3 剧情质量审核 | 人工核对摘要、人物关系、因果、标签、Top3 高光与安全边界 | 不只检查非空；逐事实和时间码抽检 | 待通过 |
| 4 跑量素材→灵感 | 真实采集/入库；灵感大屏展示；外搭/T1/T2/结构化分析/原型聚合筛选 | 证据可追溯、来源子类与题材分层准确 | 待通过 |
| 5 剧库/灵感→内容工厂 | 从两个真实页面进入工厂；检索和匹配；查看解释与质量分 | Top3、严重冲突否决、承诺兑现与跨题材正负例 | 待通过 |
| 6 过渡生产审核 | 诊断断层；制作 A 或 B；生成真实审核片；人工批准/驳回 | 时间/因果、可理解性、字幕、音量、台词和留人 | 待通过；组件实现已通过 |
| 7 项目→发布文件 | 保存/版本、入队、worker 下载合成、QC、原子发布、前端轮询 | 全状态、日志关联、QC 明细、hash 与失败恢复 | 待通过；隔离下游组件已通过 |
| 8 播放→历史复用 | 预览、人工审核、导出、我的创作/历史检索和复播 | 导出重试、版本/审核绑定、复播一致、hash 一致 | 待通过 |

异常矩阵至少覆盖：分析失败、worker 重启/超时、素材 404/损坏、无音轨、付费范围未确认、过渡未批准、QC 失败和导出重试。自动测试的 HTTP 200 或字段非空只能作为技术证据；周边返回的剧情、人物、因果、标签、高光、匹配解释、过渡脚本和历史记录都必须人工抽检。

对外状态只使用三档：`组件级已通过`、`业务链路待通过`、`内容质量待通过`。只有 8 站和异常矩阵全部留证、缺陷复测关闭后，才可把完整业务 E2E 标记为通过。

### 9.7 独立全链路工程监督与内容质量管理

长期监督角色 `/root/independent_e2e_quality_supervisor` 与实现 Agent 隔离。监督 Agent 不修改生产代码、被测提示词、系统答案或已有 golden；只在 `release-evidence/quality-supervision/` 与 `artifacts/quality-golden/` 写入协议、冻结答案、评分、差距和复测报告。主线/实现 Agent 根据报告修复，监督 Agent 使用同一 case 复测。

每个真实 case 必须执行双轨顺序，禁止倒置：

1. 在完全不查看系统答案前，由监督 Agent 假设自己是理想系统，独立生成并冻结专家标准答案。
2. Golden 至少包含结构化字段、事实与证据时间码、人物/关系/因果链、判断理由、置信度、拒绝/降级条件、推荐钩子/高光/匹配/过渡/成片方案、版本和不可变哈希。
3. 冻结后才读取系统真实输出，记录系统代码版本、模型、提示词/分析契约版本，逐字段、逐时间码、逐人物关系和逐因果链比较；HTTP 成功、字段非空或格式一致不算内容正确。
4. 输出 P0/P1/P2 差距：事实错误、遗漏、幻觉、时间码偏差、标签错误、故事不完整、留人不足、匹配错误、转场突兀、direct-cut/A/B 三模式执行偏差，以及状态、持久化、重试、QC、审核和导出问题。
5. 每项差距必须转为开发项，写明模块、预期行为、复现 case、验收样例和回归测试；修复后用原 case 和原 golden 复测，不得为过线修改 golden。确需纠正 golden 时必须新建版本并保留变更理由与旧版。

默认评分表：

| 维度 | 权重 | 核心比较 |
| --- | ---: | --- |
| 事实/证据准确性 | 25% | 人物、关系、事件、因果、台词/画面证据存在且时码准确 |
| 高光定位质量 | 15% | 起止完整、安全、可制作、无台词/动作截断 |
| 钩子留人力 | 15% | 停滑句、冲突、悬念、信息缺口与节奏 |
| 故事完整性 | 15% | 前因、推进、兑现与理解成本 |
| 钩子—正片匹配 | 15% | 承诺兑现、事实兼容、人物/空间/情绪/因果连通 |
| 转场/解说自然度 | 10% | A/B/direct-cut 选择、事实措辞、时长、声音、字幕和自然度 |
| 工程可靠性 | 5% | 状态、版本、血缘、重试、QC、审批、原子发布与复播 |

硬门禁优先于加权总分：关键人物/关系/剧情事实错误、引用不存在的证据、时间码越界、未批准资产进入成片、故事严重断裂任一出现，case 和当轮上线结论立即为 `NO-GO`。人物属于原剧或复用原剧人物资产绝不是外搭否决条件，必须以镜头来源和制作血缘判断。

可追踪台账字段固定为：`case_id`、输入资产/hash/权限、题材与来源子类、golden 版本/hash/冻结时间、系统代码/模型/提示词版本、逐字段评分、七维分数/总分、硬门禁、失败时间码/原因、P0/P1/P2、修复模块/提交或变更 ID、回归测试、复测轮次/结果、监督结论。工作循环固定为 `评测 → 缺陷 → 开发 → 同案复测 → 阈值判定`。

首轮 case 为 GH-001～GH-003 加榜首剧 `The Rise of the Lycan Queen` dramawave EP1～EP10。三条样片 golden 已按严格盲标协议冻结；没有对应原剧或系统输出的字段明确写 `pending_evidence`，不得猜测。榜首剧 playback 与 EP1～EP10 媒体可用性已验证，后续必须从真实 UI/API 入口完成落盘、入库和任务创建。语义分析的账号级阻塞已解除：已授权 `qwen-vl-max` 为主模型，`qwen3-vl-flash` 仅作降级备选；真实基线仍不得用合成剧集替代。

### 9.8 吞吐、分片与性能门禁（P0）

性能优化不得降低内容门禁。分析必须以 episode/chunk 为可缓存、可独立重试和可断点续跑单元；跨集归一化只消费已成功的结构化 checkpoint。上游流式截断只重试当前请求/chunk，不得使已成功的集数重新调用模型。同一 job 依靠原子 claim 和 lease 保证唯一所有者；不同 episode/chunk 目标并发 3～4 个 worker。

两级模型路由的硬边界：

- `qwen3-vl-flash` 只允许执行低成本粗筛、召回探针、镜头/运动候选和可编辑区间提案；输出固定 `unverified/reviewRequired`，不得进入匹配、转场或生产。
- `qwen-vl-max` 必须基于原始证据复核 Golden 覆盖区、人物关系、因果链、最终高光、Precision、匹配和转场。Max 不可用时必须 fail closed，Flash 不得冒充终审。
- 当前已完成持久 episode checkpoint：缓存键包含输入内容 hash、实际路由模型和 prompt 版本；只写结构化结果，签名 URL、原媒体和 base64 仅参与不可逆指纹且不落盘。单集失败不写缓存，流式截断只重试当前请求，跨集 reconciliation 只接收成功 checkpoint。每个 chunk 记录 model、prompt、cache hit、request count 与 wall-clock。
- PocketBase 队列侧已完成 `detail_episode` 分片契约：父 `detail` 任务幂等 fan-out、事务 claim/旋转 lease 保证唯一所有者、成功兄弟分片不重跑、全部分片成功后父任务才可进入 reconcile。当前 processor 接入仍在回归，因此尚未执行迁移和真实队列；在接入通过前不重启旧的前 10 集整体 Detail。

性能台账每个 chunk 必填：输入 hash、episode/chunk ID、model/tier、prompt version、queue/start/end/wall-clock、request/retry 次数、cache key/hit/miss、token/成本（可取时）、终态、quality gate、输出 hash 和 error class。当前审计为 `NO-GO / NOT AUDITABLE`：粗分析实测批次约 686s，Detail 累计观测约 2852s，且旧日志缺少 prompt、request、cache、token/成本与 chunk 耗时。独立监督协议与台账见 `release-evidence/quality-supervision/performance/`。

本地首轮性能门禁（使用冻结真实样本校准）：

| 阶段 | 目标 | 失败线 |
| --- | ---: | ---: |
| 前 10 集 Flash 粗筛（2 worker 冷跑） | ≤12 分钟 | >15 分钟 |
| Max Detail/关系因果终审 | ≤20 分钟 | >30 分钟 |
| 3 个 Golden Precision | ≤10 分钟 | 超时不进入成片 |
| 3 个真实钩子分析 | ≤6 分钟 | 超时进入性能缺陷 |
| 3 组匹配 | ≤3 分钟 | 超时进入性能缺陷 |
| 转场真实预览 | ≤5 分钟 | 超时不允许冒充完成 |
| 成片＋QC | ≤10 分钟且 RTF≤2 | 超时或 QC 失败不发布 |

最小可验收闭环的固定顺序为：`3 个 Golden 高光 → 3 个用户钩子 → 至少 3 组匹配 → 1 条带转场真实成片`。不再等待前 10 集全部候选精析后才启动下游。

## 10. 发布记录

### 2026-08-30 本地真实 UI 与监督基线更新

- 修复安全迁移后的 P0 回归：开发浏览器不再直连已锁定 collection API，而是仅在 `local-loopback` + `x-lumina-ui` 条件下走精确白名单 hook 网关。托管网关、ChatGPT 身份校验与 superuser-only collection 规则未放宽，未创建新凭据。真实 UI 刷新后显示“数据连接正常”、4 条跑量素材及各自 queued/0% 状态。该证据只使 BC-04 的“入库→列表可见”子步骤通过，分析和内容质量仍待验收。
- 独立监督 v2 严格盲标已冻结：`fork_turns=none` 隔离评测者只读取三个原视频、schema 和最小产品先验，`access-attestation.json` 为 violation=false；3/3 原视频 SHA 一致、schema valid，40 个证据区间均未越界。台账已切换为 `frozen_blind` v2，v1 保留 `frozen_with_prior` 审计轨迹；监督尚未读取系统输出，可开始严格双轨比较。冻结证据见 `release-evidence/quality-supervision/blind-reannotation-v2-freeze.md`。
- 本地网关不再只有列表契约：新增 fresh PocketBase 运行时验证，在不创建 superuser 的前提下，原 collection API 持续 403，受限本地入口真实完成 JSON 创建/详情/更新、MP4 multipart 首次上传、同一剧集 multipart PATCH 媒体替换、新字节读取、非白名单/非所属文件拒绝和删除。测试暴露并修复了 multipart 请求在读取 `requestInfo().body` 时使临时文件句柄失效的问题，现改为同一 handler 内以 `formFile`/`fileFromMultipart` 保存；PATCH 回归进一步覆盖前10集重复导入已有记录的真实路径，防止元数据更新但媒体仍是旧版本。
- 真实浏览器点击第一条跑量素材后，又发现原生 `<video src>` 不会附带 fetch 使用的本地信任头，导致 API/`curl` 200 但页面报加载失败。已由仅绑定本机的 Vite `/pb-local` 代理统一注入该头；新浏览器会话中播放对话框不再出现“视频加载失败”。托管构建不包含开发代理。
- 前 10 集验收入库补上数量硬门禁：传入 `episodeLimit=10` 时不仅检查 EP1 起连续，还必须恰好取得 10 集；9 集或更少不得被标为验收入库。Range GET 206/`video/mp4` 和非生产权限标记保持不变。
- 千问真实连通性状态已固定为“Plus 模型级额度策略阻断、Max/Flash 可用”：主模型设为 `qwen-vl-max`，`qwen3-vl-flash` 仅作降级；不再调用 `qwen3-vl-plus`，本任务未充值或关闭费用保护。`.env.analysis.local` 保持 gitignored、未跟踪且权限 `600`，更新与检查过程未输出凭据。
- 最新定向回归 16/16 通过，覆盖 fresh PocketBase 本地受限 CRUD/真实 MP4 上传读取、Hosted 网关与 collection 锁定、前10集精确数量门禁；本地 production build 与 `git diff --check` 通过。结论仍仅为组件和本地入口证据，不代表完整业务 E2E 或内容质量通过。
- 第一条真实 material job `30wqqaz0u3vav09` 曾有一次早期 `succeeded` 输出，但随后在启用 OCR 和更严格 material-v2 契约后变为权威终态 `failed`；独立监督 v1 据此判定 `NO-GO 22/100`（5 个 P0、3 个 P1、1 个 P2）。早期成功只能作历史组件证据，不是当时的业务终态。
- 已修复 worker 启动、失败重试原子清理、来源/层级证据门禁、契约缺失诊断、只用已验证客观事实的摘要重建，以及 75 秒以下素材的紧凑语义路径与内容哈希缓存。正式 UI 重试入口→claim→worker→回写的同案复测已于 2026-08-31 成功：`qwen-vl-max`、prompt `material-v2-20260830.1`、job `succeeded/100%`，输出 3 个可验证客观事实、3 个分段、摘要仅引用 1 个已验证 fact。业务投影保持 `TX`、来源未知、hook=0、`needs_review`、production gate blocked；系统明确报告事实证据、推断引用、摘要完整性和来源血缘缺口。这只证明工程闭环恢复；内容质量仍是 `NO-GO/待独立 v2 复评`。
- 独立监督 v2 沿用未改写 golden v1 完成复评：工程执行 `PASS_WITH_TRACEABILITY_RESIDUAL`，内容质量 `FAIL`，硬门禁 `NO-GO`，范围归一分 22→28。已修 job 永久失败、同剧外搭幻觉和 T1 误投影；仍存在虚假完整、无有效 hook、核心反转因果链缺失、悬空/零时长证据、CTA 误标高光、根来源状态不一致和历史失败前任不可追溯。证据见 `release-evidence/quality-supervision/cases/QS-MAT-LYCAN-13/gap-report-v2.md`。
- 已先收口监督 v2 新增的确定性发布缺陷：`materialFields.confidence` 改为受人物、关系、hook、评分完整度及 quality gate 共同约束，gate 未通过时上限 49，并显式发布 `productionStatus=blocked`；未完成原剧镜头/血缘匹配时 root `sourceAttribution` 改为 `pending`。回归已通过，尚未使用新模型请求重跑内容。
- 同一真实样本已通过正式 UI 重试→worker 路径以 `force_semantic_refresh=false` 复用内容哈希缓存，没有新增模型请求。真实投影已产生 `15.1–23.46s` 的 `identity_reveal_response` 待审 hook；`24.48–26.56s` 及 `29.367–34.504s` 促销段从 cliffhanger/highlight 移出并归入 CTA；故事完整性改为 `不完整/unverified`，来源 `pending`，置信度 49，生产状态 blocked。
- 独立监督 v3 沿用原 golden 复评：工程 PASS、生产阻断 PASS、范围归一分 28→66；内容仍 `NO-GO`，当时唯一 P0 为被拒绝 fact 仍被 segment/transition 消费。已将无效 observation/inference/segment/transition 移入 `review.rejectedClaims`，当前可消费 observations/segments/timeline 只引用存在的 verified `fact-002`，transitions 为空，CTA 保留直接时码证据但 `basedOnFactIds=[]` 且 `storyCandidate=false`。完整性旧的“完整流程”证据文案已被替换为引用 `fact-002` 的不完整诊断，v4 独立复评进行中。
- 独立监督 v4 确认证据图闭包 P0 已关闭，范围归一分 66→70；工程和安全阻断 PASS，内容仍 `NO-GO`。剩余内容缺口为人物/关系及“发令→回应→旁观者反应”链不足、hook 结束动作证据不足、格式证据与 retry predecessor 审计残项。
- 已将带 `framePath` 的 OCR 显式定义为 `ocr_frame/sampleType=point`，与 ASR/OCR 持续区间分开校验。基于真实 ASR 命令、身份称号和服从回应，系统当前发布 3 个 verified facts、2 个角色和 1 条 `mention_response` 相邻对白关系；没有把尚未由程序验证的“跪地/震惊”画面写成事实。hook 结束已从 23.46s 吸附到 23.9s 真实镜头边界，但 action/semantic boundary 仍 unverified，必须人工审核。quality gate 已移除已清理边的过时失败文案，具体隔离原因仍保留在 `rejectedClaims`。v5 独立复评进行中。
- 独立监督 v5 发现两个新 P0：早期规则误把 0–2.86s 壮汉发令者与 20.9s 回应者相连；证据图闭包未覆盖 `bodyFormat`、`hookSourceStatus`和 `value.risks` 等外围发布字段。该轮因此仍 `NO-GO`，范围归一分 69。
- 两个 v5 P0 已修复并经正式 worker 回写：撤销错误命令因果，只保留 16.8–20.04s 点名者与 20.9–21.86s 回应者之间的 `mention_response/相邻对白层` 关系，不声称已验证跪地或旁观者反应；证据闭包扩展到人物、关系、bodyFormat、来源、风险和灵感字段。当前 material format 因主体结构证据不足已降为“未确定”，不再弱证据发布 `EPISODE_SPLICE`。v6 独立复评进行中。
- 独立监督 v6 确认 v5 两个 P0 均已关闭：错误跨事件关系已撤销，递归扫描全部可消费 `basedOnFactIds` 后 invalid refs=0，格式安全降为“未确定”。工程 PASS、安全阻断 PASS，范围归一分 69→77。内容仍 `NO-GO`，但已无 P0 数据污染/虚假因果；剩余为 P1/P2：跪地服从与旁观者震惊的视觉事实未经程序验证、点名者缺说话人/画面归因、23.9s action/semantic boundary 仍需审核，以及 ASR/OCR 文本融合与可疑 `Wreckus` 风险标签。证据见 `release-evidence/quality-supervision/cases/QS-MAT-LYCAN-13/gap-report-v6.md`。
- 已新增独立 `paid-ad-material-visual-event-verification` 契约、内容哈希缓存和确定性发布门禁。动作/反应必须引用至少两个真实、有时序差的帧；单帧只能证明状态；说话人必须有声纹/唇动/连续发言证据；相邻镜头不能证明因果。真实 `qwen-vl-max` 聚焦调用已缓存，系统对模型候选再校验后为 accepted=0、rejected=4、speakerVerified=0，因此没有将“跪地/震惊/似乎说话/Killian 身份”写入事实层。
- 正式本地 UI API 强制重试（`force_semantic_refresh=false`）→同 ID 队列→worker→PocketBase 回写已通过：job `30wqqaz0u3vav09` 为 `succeeded/100/completed`，material 保持 `needs_review`、quality gate=false、confidence=49、production blocked。`visualEventVerification` 已作为一等结果持久化；所有 rejected 项强制 `unverified/reviewRequired=true`，0 条通过时 action/semantic boundary 强制 unverified，建议边界与正式 hook `15.1–23.9s` 一致。独立监督 v8 确认 v7 两个 P1 契约矛盾关闭；v9 又确认 speaker P1 关闭：缺声纹/唇动归因的两个人物候选与 `mention_response` 关系均为 unverified，只保留已验证的对白事实，无新泄漏，范围分 77→79。内容仍 `NO-GO`，不得宣称真实跪地服从或旁观者震惊已自动验证。剩余 P1 为如何用更稠密的短视频时序证据形成 accepted 动作/反应事实。
- 最新回归更新为 Node 138/138、Python 151/151、production build 和 `git diff --check` 通过。这仍只是组件、单样本业务与监督证据，不代表 40 条跨题材数据、前 10 集分析、匹配、过渡、真实成片或 BC-01～BC-08 已通过。
- 真实启动同时暴露并修复两项 worker P0：FFprobe 检测误用 `-perm -111`，拒绝当前用户可执行但 group/world 不可执行的项目二进制；启动器默认系统 Python 3.14，而预检使用已验证的 Python 3.12 分析 venv。现统一为 owner-executable FFprobe，并默认复用 `.runtime/analysis-venv/bin/python`；首次失败通过正式 job retry API 留痕后同 ID 复测成功。启动器定向测试 5/5、analysis 预检 0 失败/0 警告、全量 Node 132/132 与 Python 132/132 通过。
- 2026-08-31 已从真实 Chrome UI 执行“数据源管理 → playback → 前10集验收入库”，将 `The Rise of the Lycan Queen` dramawave 源 EP1–EP10 完整落盘至剧目 `s2mhnmm4t9q9yud`。真实 UI 终态为剧库 1 部、正片已获取 10/51、EP1–EP10 均有持久化 MP4，EP1 播放器显示正在播放真实片源。每集已记录 PocketBase ID、字节数、ffprobe 时长和 SHA-256，证据见 `release-evidence/2026-08-31/lycan-first10-intake.md`。生产 HTTPS/loopback 代码门禁未修改；上游明文 HTTP 仅在本轮用户授权验收中经临时本机桥接使用，不记录 header/签名 URL。BC-01 的查询、入库、列表、详情和 EP1 播放子链已通过，逐集完整人工播放质检仍待完成。独立监督正在不查看系统输出的条件下冻结 EP1–EP10 golden，冻结前不启动分析。
- EP1–EP10 独立 Golden 已在系统分析启动前冻结：case `QS-DRAMA-LYCAN-DW-EP01-10`、version `v1`、SHA-256 `4f36c1df4593442974ac25ee9a3cec64ac10cff537894035eee1633389c96872`，10 集与 Top3 齐全，`freeze-manifest.systemAnalysisMayStart=true`。监督声明未读取任务表、系统分析结果、实现提示词或旧剧集 Golden；EP2 无音轨，其他无法确认的对白/说话人/跨集连续性均已降级为 pending，不以猜测补齐。
- Golden 冻结后从真实 Chrome UI 提交“粗解析 10 集、重点集 1–10、候选区间自动精析”。首次提交暴露 P0：剧集分析本地鉴权只读 `requestInfo().headers`，而 PocketBase/Go 将 Host 提升至 `e.request.host`、自定义头需从原始 Header 读取，导致合法本地 UI 被误拒。现已按已验证的 material 网关方式修复，未放宽 `local-loopback`、loopback Host、Origin、显式本地头或 gateway token 门禁；同一 UI 动作回归后任务中心出现 10 个真实任务。
- 已并行启动 2 个 drama worker；启动瞬间队列为 queued=8、active=2、failed=0、attention_required=false。主模型和 worker 默认值均为 `qwen-vl-max`；`qwen3-vl-flash` 仅作为显式降级备选，`qwen3-vl-plus` 保持模型级 `FreeTierOnly` 记录且不再调用。相关 launcher 契约 10/10、analysis 运行预检 0 失败/0 警告。该记录只表示 BC-02 正在真实运行，不表示粗/细/精分析或内容质量已经通过。
- BC-02 首轮细分析曾因图像请求体达 5,160,400 字符被上游 HTTP 400 拒绝。已将 Detail 证据固定为每集最多 8 帧、256px/JPEG55、全请求 Base64 硬预算，并保留首尾及 motion burst；同案真实重试已进入 `qwen-vl-max` 语义阶段。此次失败/重试是真实异常链证据，但终态和内容质量仍待确认。
- 独立监督对首轮已落库 Detail 按冻结 Golden 完成对照：工程为 PASS，内容范围分 48/100，硬门禁 `NO-GO`。主要 P0 为 EP2 无音轨时过度推断预言/母子/意图，EP3 母女关系错判，EP9 “No. Not Tiffany.”否定语义反转，以及 Golden Top3 高光 0 命中。该结论取代“字段非空即通过”，必须修复后用同一 Golden 复测。
- 针对上述 `NO-GO` 已加入否定极性/弱血缘声明拦截、无音轨的角色与关系候选降级、不安全事实不进入故事合成，并修复 Detail→Precision 的循环门禁。Precision 新增最多 16 帧、320px/JPEG60、Base64 ≤240k 的硬预算；正式区分不可改写的 `eventInterval`、送精析的 `precisionInterval` 与后续待验证的生产边界。零 eligible 不再伪装完成，重复 ensure 不再覆盖 queued/running 状态，每个 Detail analysis id 拥有独立 Precision 任务世代。定向 Python/Node 回归、语法检查和 `git diff --check` 通过；真实同案终态待本轮 worker 落库。
- 独立监督 v2 使用原 Golden 复测后，范围分 48→60，工程 PASS、内容仍 `NO-GO`。已关闭 EP2 无音轨幻觉发布与 EP3 母女关系误发布；剩余 P0 为 EP9 仍把 “No. Not Tiffany.” 写成信任动摇，且混入两条 EP10 证据驱动 result/carryOut。Golden Top3 时间覆盖仅 1/3、可直接制作 0/3；16 条候选的 action/dialogue/semantic/shot 边界全部 unverified。证据见 `release-evidence/quality-supervision/cases/QS-DRAMA-LYCAN-DW-EP01-10/gap-report-v2.md`。
- 针对 v2 剩余 P0，已新增单集事实证据所有权强校验：EP N 的 `coreFacts` 不得引用其他集证据；summary/cause/action/result/carryOut/relationshipChange 从本集合法事实重建，没有安全事实时整体降级待审。旧版生成的 16 个 Precision 任务已经由正式 pause API 暂停，未继续调用模型；真实 UI 已再次提交包含 motion-burst 取证的 Detail v3，当前运行中。
- Detail v4 已从真实 UI/worker 落库：EP9 不再引用 EP10 证据，`relationshipChange=未验证`，该 P0 确定性关闭；候选从 14 增至 20。但 Golden Top3 仍仅 EP7 时间覆盖，EP4 27–54s 和 EP8 75.5s–片尾未成为叙事候选，因此 19 个 Precision 任务已由正式 pause API 暂停，没有用低召回候选扩大模型消耗。
- 为避免 Detail 模型漏报直接阻断稠密精析，已新增“召回探针”：实测 motion burst 和未覆盖片尾可生成显式 `motion_recall_probe/ending_recall_probe`，它们只获得 Precision 资格，不声称 audienceQuestion/narrativePromise，`qualityGate=false`、`productionGate=unverified`、`reviewRequired=true`，精析后才决定是否为真实高光。同时模型自报 safeStart/safeEnd 降为 proposal，必须由实测 ASR/FFmpeg shot 重算，无独立动作证据时不得 verified。Detail v5 已从真实 UI 提交并由新 worker 领取。
- Detail v6 暴露 worker 实际帧时间码为 `{start,end}` 而探针最初只识别数字，导致片尾探针未生成；已修复并增加真实结构回归。v6 同时完成了第一个真实 Precision job `a0ffz3hlytxp1on`：`qwen-vl-max` 成功生成 2 个高分 hook，但实测边界重算后 action/shot/semantic 仍 unverified、`productionReady=false`，证明模型自报边界没有绕过安全门。
- 为防止全片最强 motion 偏向片头/片尾转场，召回扫描已改为在压缩送模前对每集时间线四分段，每段最多生成一个持续运动探针；已有叙事事件与探针的去重改为比较 `eventInterval` 而不是宽泛 `precisionInterval`。v7 首试在 83% 跨集归一化时因上游流式响应只返回 68 字符、未形成完整 JSON 而真实失败；已使用正式 retry API 同 job 重试，未直接写数据库。流式不完整 JSON 现已在单次语义请求内按既有尝试预算做短退避重试，其他语义校验错误仍关闭失败。
- 按吞吐 P0 要求，旧单体 Detail job 已暂停且 drama worker 已停止，不再采用“持续轮询大 job + 全量重跑”。checkpoint/跨集只读缓存的 Python 回归为 129/129 通过；真实多 worker shard 仍以 processor 接入和 fresh runtime 回归为准，不能以契约测试冒充完成。
- 三条真实用户素材已并行领取；首轮新增终态为 1 succeeded、2 failed。成功任务具备 verified 中文摘要与 2 条 observations，但 hook=0，仍属内容质量待验。两条失败分别定位为：确定性聚合丢失 segment observations；OCR 点证据回传丢失本地 frame lineage。已修复为本地合并/命名空间化 verified facts、只从匹配的源 OCR 恢复 `framePath`，不放宽任意零时长证据；129/129 回归通过。两条任务已通过正式本地重试入口以 `force_semantic_refresh=false` 重新排队，必须复用已有语义缓存，不允许再次付费调用完整分段模型。
- 本轮性能事实：三条素材首轮并发；已知失败任务 wall-clock 分别约 76.47s 与 5.15s，第三条成功耗时待从终态日志补齐。当前缓存命中率和模型调用数只有在复测终态日志落库后才填，不以推测值代替。性能监督仍为 `NO-GO / NOT AUDITABLE`，最小闭环尚未完成。
- 本地 PocketBase 已应用 `1787571100_add_detail_episode_shards.js`；原暂停父任务 `vlpc5yoodbf1d43` 经正式 `retry-detail` 入口生成 10 个 `detail_episode` 子任务，随后由 4 个独立 drama worker 各自原子 claim 不同 episode。该记录证明真实 fan-out/唯一租约已启动，终态、fan-in、内容质量和性能门禁仍待实际结果。
- 素材复测第一次仍由修复前已常驻的 Python worker 执行，进程没有热加载代码，因此两条同错误失败；这不是新实现的复测结论。已停止旧 worker、重启本地服务并用新进程重新从正式入口排队，保持 `force_semantic_refresh=false`。后续只以新进程终态为准。
- 分片真实链路已完成：10/10 `detail_episode` succeeded，父 `detail` 仅在全部 checkpoint 成功后由唯一 worker claim，并以 `checkpointOnly=true` 完成 Max fan-in，最终 `succeeded/100%`。父阶段实测 `checkpointCount=10`、Max 请求 12 次、checkpoint cache hit 10/10、fan-in wall-clock 346.235s（约 5.77 分钟），低于 20 分钟目标；产出 10 集剧情、6 人物、6 关系、26 个高光候选。内容正确性仍以独立 Golden 对照为准。
- fan-in 初次不能领取的根因不是 worker 缺失，而是旧 worker 完成日志覆盖了 `parent_job` 可变字段，使 shard summary 找不到父子关系。已改为优先读日志、缺失时从不可变 idempotency key 恢复父血缘；episode number 同样从关系记录 fail-safe 恢复。修复后只续跑父汇总，10/10 分片没有重跑。
- fan-in 完成后 4 个 worker 自动领取了 20 个 Precision：16 个在主线发现前快速成功，4 个已立即通过正式 pause API 中止。该行为超过“只跑 3 个 Golden”预算，是新的调度 P1；后续必须为 Precision 增加批次上限/Golden 优先级，不能依赖人工抢停。
- 独立监督确认此前成功的 3 条 material job 并非用户提供的 3 条 Golden：任务 SHA 为 `da4d7f…/3b12aa…/a7c5a1…`，而冻结 Golden 为 `406ba7…/eddfbf…/0b82e9…`，3/3 输入身份不一致，因此不得逐字段评分。三条非 Golden 输出均 `hook_count=0`、quality gate=false、blocked，安全阻断正确但核心召回为 P0。
- 真实 Golden 原片为 199–245MiB，常规 multipart 入口首轮返回 413/502，暴露大文件入库 P0。已新增仅 `local-loopback` 可用、配置根目录约束、扩展名白名单和 SHA-256 必填的本地大文件导入 API；worker 下载后必须重新计算 SHA 并与入库 identity 一致，否则永久 media failure。三条原片现已通过正式本地 API 入库，material IDs `j6up90l4zn2sxty/gtht4bezg3cr96x/57jtepn5krbr9e0`，3/3 hash match 且自动生成真实分析任务；3 个 material worker 已并行 claim。该本地适配器不是托管上传方案，预生产仍需对象存储/分片上传设计。
- 预生产独立审计仍为 `NO-GO`：托管拓扑、持久存储、同点备份恢复、真实告警/容量、迁移回滚和自动部署状态尚未留证；当前磁盘约 5.4GiB 可用且 98% 使用，是立即 P0。完整清单与 fail-closed 预检见 `docs/预生产上线准备与回滚.md` 与 `release-evidence/preprod-readiness/`；本轮未部署、推送或操作凭据。
- Precision 自动放量已改为默认最多 3 个候选，支持 `LUMINA_PRECISION_AUTO_LIMIT=1..20` 显式调节；排序优先 Golden/人工标记，其次质量分和证据完整度。超额候选标为 `deferred/reviewRequired`，不再自动建队，且不能把“3 个完成、其余延后”冒充整体完成。新增放量契约测试 2/2 通过。
- 为三条 Golden 使用冻结 v2 候选区间创建了正式人工钩子草稿：QS-GH-001 `0–39s`、QS-GH-002 `0–30s`、QS-GH-003 `0–24s`。三条均为 `boundary_status=unverified`、`review_status=needs_review`，来源仅标“疑似外搭/外搭来源待确认”；未使用人物连续性否决困难正例，也未越过独立边界审核、权限和生产门禁。
- 3 个长视频 material worker 在宿主进程回收后留下同一 job 的未来租约；已通过正式 material retry API 以 `force=true, force_semantic_refresh=false` 清理租约并原子重领，继续复用已有扫描/ASR/OCR/语义 checkpoint。该故障计入 worker 重启/断点恢复 E2E，不新建任务、不直接写库。
- 曾尝试的“认证网关内存缓冲 512MiB multipart”方案已撤回：它既不能绕过 PocketBase 请求体上限，又会制造应用进程内存峰值。托管大文件必须走对象存储直传/分片上传；当前只有根目录受限的本地验收适配器通过。

尚未发布。本文档中的“功能上线”目标不构成生产部署授权；真正的生产部署必须在当前任务中获得用户明确授权。
